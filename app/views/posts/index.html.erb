<h1>Posts</h1>
<p>Voici tous les posts :</p>

<ul>
  <% @posts.each do |post| %>
    <li><%= post.content %>
      (
        <%= link_to "Show", post_path(post) %>
        <%= link_to "Edit", edit_post_path(post) %>
        <%= link_to "Delete", post_path(post), data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
      )</li>
  <% end %>
</ul>

<%= link_to "Créer un post", new_post_path  %>
